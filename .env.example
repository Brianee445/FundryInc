NEXT_PUBLIC_API_URL=http://localhost:8000

# Same Google Cloud OAuth client as the backend's GOOGLE_CLIENT_ID — this one
# is intentionally public (it's embedded in the page for Google Identity
# Services to read), unlike a client secret, which is never used here.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
